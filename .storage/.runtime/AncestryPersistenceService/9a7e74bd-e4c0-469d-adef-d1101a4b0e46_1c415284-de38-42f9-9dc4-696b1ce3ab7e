{"Target":{"Guid":"9a7e74bd-e4c0-469d-adef-d1101a4b0e46","Ancestry":[{"IsSelected":true,"Selector":"<html app=\"chrome.exe\" title=\"&#x963F;&#x5C14;&#x6CD5;&#x7F16;&#x7A0B;\"/>","ValidAttributes":{"app":"chrome.exe","title":"阿尔法编程","url":"https://sxgxy.alphacoding.cn/courses/12182/learn/28pn34cj0vffl0mcbyjv"}},{"IsSelected":true,"Selector":"<webctrl parentid=\"left-solution-content\" tag=\"PRE\"/>","ValidAttributes":{"parentid":"left-solution-content","tag":"PRE","aaname":"# ls = [] # while temp := input(): # \tls.append(temp.split()) # print(sorted(ls,key=lambda x:float(x[1][:-1]) * 1000 if x[1][-1] == 't' else float(x[1][:-2]))) # 输入一系列动物名和其重量，重量单位可能是kg，也可能是t，动物名、与重量间空格分隔，重量数值与单位间无分隔 # 按重量由小到大排序输出 ls = [] while True: \ttemp = input() # 判断输入的是否为回车 \tif temp == '': # 输入回车时结束循环 \t\tbreak \tls.append(temp.split()) # 将接收的字符串根据空格切分，增加到列表中 print(sorted(ls,key=lambda x:float(x[1][:-1]) * 1000 if x[1][-1] == 't' else float(x[1][:-2]))) # 排序 关键字是列表第2个元素的值，其值要根据最后一个字符是否为t判定其单位是否为吨，此时需转为公斤  *","class":"code-solution overflow-x-auto select-none","css-selector":"body>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>pre","innertext":"# ls = [] # while temp := input(): # \tls.append(temp.split()) # print(sorted(ls,key=lambda x:float(x[1][:-1]) * 1000 if x[1][-1] == 't' else float(x[1][:-2]))) # 输入一系列动物名和其重量，重量单位可能是kg，也可能是t，动物名、与重量间空格分隔，重量数值与单位间无分隔 # 按重量由小到大排序输出 ls = [] while True: \ttemp = input() # 判断输入的是否为回车 \tif temp == '': # 输入回车时结束循环 \t\tbreak \tls.append(temp.split()) # 将接收的字符串根据空格切分，增加到列表中 print(sorted(ls,key=lambda x:float(x[1][:-1]) * 1000 if x[1][-1] == 't' else float(x[1][:-2]))) # 排序 关键字是列表第2个元素的值，其值要根据最后一个字符是否为t判定其单位是否为吨，此时需转为公斤  *","isleaf":"1","parentclass":"exercise-solution border-l-8 rounded-lg bg-success-50 border-success-500 px-6 py-5 transition-shadow shadow hover:shadow-md","visibleinnertext":"# ls = []\n# while temp := input():\n# \tls.append(temp.split())\n# print(sorted(ls,key=lambda x:float(x[1][:-1]) * 1000 if x[1][-1] == 't' else float(x[1][:-2])))\n# 输入一系列动物名和其重量，重量单位可能是kg，也可能是t，动物名、与重量间空格分隔，重量数值与单位间无分隔\n# 按重量由小到大排序输出\nls = []\nwhile True:\n\ttemp = input() # 判断输入的是否为回车\n\tif temp == '': # 输入回车时结束循环\n\t\tbreak\n\tls.append(temp.split()) # 将接收的字符串根据空格切分，增加到列表中\nprint(sorted(ls,key=lambda x:float(x[1][:-1]) * 1000 if x[1][-1] == 't' else float(x[1][:-2])))\n# 排序 关键字是列表第2个元素的值，其值要根据最后一个字符是否为t判定其单位是否为吨，此时需转为公斤\n\n*"}}]},"Anchors":[{"Guid":"1c415284-de38-42f9-9dc4-696b1ce3ab7e","Ancestry":[{"IsSelected":true,"Selector":"<html app=\"chrome.exe\" title=\"&#x963F;&#x5C14;&#x6CD5;&#x7F16;&#x7A0B;\"/>","ValidAttributes":{"app":"chrome.exe","title":"阿尔法编程","url":"https://sxgxy.alphacoding.cn/courses/12182/learn/28pn34cj0vffl0mcbyjv"}},{"IsSelected":true,"Selector":"<webctrl css-selector=\"body&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div&gt;div\" parentid=\"left-solution-content\" tag=\"DIV\"/>","ValidAttributes":{"css-selector":"body>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div>div","parentid":"left-solution-content","tag":"DIV","class":"explain-caption flex items-start justify-between mb-4","innertext":"答案","isleaf":"0","parentclass":"exercise-solution border-l-8 rounded-lg bg-success-50 border-success-500 px-6 py-5 transition-shadow shadow hover:shadow-md","visibleinnertext":"答案"}}]}]}